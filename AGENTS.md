# AGENTS.md instructions

1. 不要使用本机 Python，一律使用虚拟环境 Python。
2. 允许与 git 相关的一切命令。
3. 输出文档中不使用 Courier 字体。
4. 如果使用 LibreOffice，必须用系统 `soffice` 绝对路径，并在非受限环境/授权环境下跑。
5. 本项目是试验性项目，禁止过度治理：
   - 当前唯一产品合同是：沪深300、中证1000、创业板指、科创50只使用 Stan Weinstein
     周线30周方法评估 Stage；沪深A股个股只使用 Stan Weinstein 与 Mark Minervini 两种
     可辨认的独立方法形成观察池。程序只缩小范围，不预测核心目标、不合成跨方法总分，也不
     输出自动买卖指令；最终分析、重点观察和放弃由用户决定。O'Neil、VCP、ETF、市场加权分
     及旧实验结果不进入正式产品和日常运行路径。
     新版唯一用户交付面是网站；不开发或维护 Excel、离线 HTML、CSV 等离线报告和多文件
     发布包。后台命令只允许承担采集、计算、迁移和诊断，不作为产品展示入口。
     优先交付可运行功能、真实数据结果和用户可观察反馈，不以计划、工作包、审计、文档数量
     或测试数量代替产品进展。
   - 默认采用最小充分方案和与风险相称的验证。低风险、小范围修改不得扩张为多轮规划、
     多代理审查、全仓重构或独立验收工程。
   - 除正式数据库写入、数据迁移、删除、发布、权限安全等高风险操作外，计划、审计、验收和
     治理材料的投入原则上不得超过单项任务总投入的 20%；预计超出时必须停止并征求用户意见。
   - 同一结论只保留一个权威计划和一个进度/证据入口；禁止为同一状态反复创建总结、台账、
     验收报告和重复终审。已有证据足够时直接引用，不重复生成。
   - 项目验证优先使用历史真实数据，采用严格 as-of/PIT、时间切分或 walk-forward 方式，
     明确复权尺度、基准、样本覆盖和缺失边界；缺少点时证据的数据必须标记为不足或仅作探索，
     禁止未来数据泄漏、跨日期拼接或把近似重建冒充正式证据。
   - 默认不做影子运行，也不以累计未来真实交易日作为项目完成条件。既有影子运行代码和证据
     只作历史记录，不再投入开发、审计或日常维护；除非用户以后明确重新授权，不得恢复。
   - 优先运行受影响范围测试；只有核心合同、跨模块改动、正式写入或发布候选才运行全量回归。
     测试通过只证明机械正确，不自动触发新的审计或文档工作包。
   - 默认由一个代理完成任务；只有独立、并行且能显著降低风险或时间的子任务才使用子代理。
   - 外部时间、授权或实时样本不得成为默认开发主线；优先改用合格历史数据验证。确实无法
     历史验证时，明确报告一次并停止，不得用更多 token 制造替代性工作。
   - 上述精简不取消必要安全底线：正式写入、迁移、删除和发布仍须有精确范围、备份/回滚与
     最小验证，但不得借安全名义扩张无关治理。
6. 发布与数据同步约定：
   - 功能先在本地开发并使用项目虚拟环境验证。本地验证通过后不自动发布；只有收到用户明确指令，才同步到 ECS 并更新 GitHub。
   - ECS 上的三个运行 SQLite 数据库是线上权威数据。日常部署只同步代码和配置，不再从本地上传、覆盖或回滚 ECS 数据库。
   - 需要数据库结构变更时，必须使用 ECS 现有数据执行显式迁移，并保留用户库备份和回滚路径。
   - GitHub 只保存已验证的代码、配置模板和文档；不推送运行数据、密钥、会话或个人数据。
